# serial2midi GUI

### Build steps

Requires Python installed.

1. `python3 -m venv venv`
2. `source venv/bin/activate`
3. `pip install -r dependencies.txt`
4. `chmod +x build.sh`
5. `./build.sh`

The binary will be available at `dist` folder.

Based on: https://github.com/Jikstra/serial2midi
